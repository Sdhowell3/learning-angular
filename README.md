# Scotts Learning Angular

## Getting started
Make sure all libraries are properly installed by running the command
```
npm install
```
Then merely run the following command to start gulp building the typescript files and listen for changes.
```
npm start
```
